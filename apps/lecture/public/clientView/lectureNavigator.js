//var Vue = require('vue')
//var LectNav = require('./vue/lecture-container-navigator.vue')


// *** NOT required as well.... (seems like nothing to be defined here)
// since it is a component; export it as a vue-component
/*
Vue.component('lecture-navigator', {
  render: function(createElement) {
    return createElement(LectNav)
  }
});
*/

/*
// main.js
var Vue = require('vue')
var LectNav = require('./vue/lecture-container-navigator.vue')

//
// create an app using #lecture-navigator
//  (not my case, since this is just a component;
//  not starting point of an app)
//
new Vue({
  el: '#lecture-navigator',
  render: function (createElement) {
    return createElement(LectNav)
  }
})
*/
